# PostgreSQL connection (use .env locally, never commit secrets)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/crediq?schema=public

# ML microservice URL used by the Express orchestration API
ML_SERVICE_URL=http://localhost:8000

# API ports
PORT=3000
ML_PORT=8000
